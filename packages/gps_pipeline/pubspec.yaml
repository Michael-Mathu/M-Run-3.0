name: gps_pipeline
description: A pure Dart pipeline for filtering, smoothing, and gating raw GPS telemetry.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  collection: ^1.18.0
  http: ^1.6.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
